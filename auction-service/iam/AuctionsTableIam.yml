AuctionsTableIAM:
    Effect: Allow
    Action:
        - dynamodb:PutItem
        - dynamodb:Scan
    Resource:
        - ${self:custom.AuctionsTable.arn}
        # - arn:aws:dynamodb:us-east-1:747441403622:table/AuctionsTable

        # - arn:aws:dynamodb:#{AWS::Region}:#{AWS::AccountId}:table/AuctionsTable
